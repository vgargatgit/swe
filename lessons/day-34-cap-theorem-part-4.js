// Day 34 full lesson, part 4 of 4.
window.FULL_LESSONS['day-34-cap-theorem'].sections.push(...[{"title":"48. Partitioning the data can change the CAP problem","body":"<p>Suppose users are geographically assigned:</p>\n<div class=\"code-block\"><span class=\"code-label\">Partitioned ownership</span><pre>India users owned by Mumbai\nUS users owned by Virginia</pre></div>\n<p>Each user has one authoritative region.</p>\n<p>Now network partition:</p>\n<div class=\"code-block\"><pre>Mumbai cannot reach Virginia</pre></div>\n<p>India operations continue in Mumbai.</p>\n<p>US operations continue in Virginia.</p>\n<p>No conflict occurs because ownership is disjoint.</p>\n<p>You have avoided cross-region coordination by changing:</p>\n<div class=\"code-block\"><pre>data ownership</pre></div>\n<p>This is another powerful pattern:</p>\n<div class=\"callout\"><p>Avoid consistency conflicts by partitioning authority.</p></div>\n<p>The system is effectively strongly consistent per key/region rather than globally.</p>","diagram":"flowchart TD\n  Router[Route by authoritative region] --> India[India keys owned by Mumbai]\n  Router --> US[US keys owned by Virginia]\n  India --> Mumbai[Mumbai writes India keys]\n  US --> Virginia[Virginia writes US keys]\n  Mumbai -. regional partition .- Virginia\n  Mumbai --> ContinueIndia[India-owned keys continue]\n  Virginia --> ContinueUS[US-owned keys continue]"},{"title":"49. Per-key consistency","body":"<p>Many distributed databases offer strong consistency for:</p>\n<div class=\"code-block\"><pre>one key or partition</pre></div>\n<p>while not providing a globally ordered transaction across all keys.</p>\n<p>Example:</p>\n<div class=\"code-block\"><span class=\"code-label\">Per-key leader</span><pre>wallet 123\n    one leader\n    ordered mutations</pre></div>\n<p>Another wallet:</p>\n<div class=\"code-block\"><span class=\"code-label\">Independent key leader</span><pre>wallet 456\n    different leader</pre></div>\n<p>Operations across the two may not share a global order.</p>\n<p>This scales better because coordination is localized.</p>\n<p>Strong consistency does not always require one global leader for the entire database.</p>","diagram":"flowchart TD\n  Router[Partition router] --> W123[Wallet 123 leader A]\n  Router --> W456[Wallet 456 leader B]\n  W123 --> Order123[Ordered mutations for wallet 123]\n  W456 --> Order456[Ordered mutations for wallet 456]\n  Order123 --> Local[Strong consistency localized per key]\n  Order456 --> Local"},{"title":"50. Interview question: Explain CAP without saying “pick two”","body":"<p>A strong answer:</p>\n<div class=\"callout good\"><p>CAP says that when a distributed system experiences a network partition, it cannot guarantee both linearizable consistency and availability for every request. If it preserves consistency, some nodes must reject or delay operations because they cannot prove they have current authority. If it preserves availability, isolated nodes may continue serving operations and therefore temporarily diverge.</p><p>Partition tolerance is not normally optional in a distributed system, so the useful design question is how each operation behaves during partition rather than choosing two arbitrary letters.</p></div>"},{"title":"51. Interview question: What does consistency mean in CAP?","body":"<div class=\"callout good\"><p>CAP consistency is generally interpreted as linearizability: successful operations appear to occur atomically in one global order that respects real-time ordering. If a write completes before a later read begins, that read must observe the write. It is unrelated to the \"C\" in ACID consistency, which refers to preservation of application/database invariants.</p></div>\n<p>This distinction is an excellent interview signal.</p>"},{"title":"52. Interview question: Is PostgreSQL CP or AP?","body":"<p>A better answer than assigning a label:</p>\n<div class=\"callout good\"><p>It depends on the deployment. A single PostgreSQL instance does not meaningfully face distributed CAP trade-offs. In a replicated high-availability cluster, if only the quorum-authorized primary accepts writes and a minority side refuses them during partition, the write path behaves CP. Asynchronous read replicas may still provide stale reads, so the application's read path can expose weaker consistency. Configuration and routing determine the behavior.</p></div>"},{"title":"53. Interview question: Would you choose CP or AP for payments?","body":"<div class=\"callout good\"><p>For authoritative payment state, idempotency records, balances and ledger invariants, I would generally prefer CP behavior: if the system cannot establish a single authoritative writer or sufficiently current quorum, I would rather reject or defer the operation than risk double spending or duplicate financial state.</p><p>Other payment-related views such as transaction history, analytics or notifications can often use eventually consistent replicas because stale presentation does not change the authoritative financial outcome.</p></div>"},{"title":"54. Interview question: Give an AP use case","body":"<p>A shopping cart is a classic example.</p>\n<p>Two disconnected regions may both accept additions.</p>\n<p>Later:</p>\n<div class=\"code-block\"><span class=\"code-label\">Disconnected cart state</span><pre>cart A = {book}\ncart B = {headphones}</pre></div>\n<p>Merge:</p>\n<div class=\"code-block\"><span class=\"code-label\">Merged cart</span><pre>cart = {book, headphones}</pre></div>\n<p>Availability is valuable and conflict semantics are manageable.</p>\n<p>Other good examples include:</p>\n<ul><li>social likes</li><li>presence</li><li>recommendation state</li><li>many analytics counters</li></ul>\n<p>provided exact instantaneous values are not business-critical.</p>"},{"title":"55. Interview question: What is PACELC?","body":"<div class=\"callout good\"><p>PACELC extends CAP by noting that distributed systems face a trade-off even when there is no partition. During a partition they choose between availability and consistency; otherwise they often choose between lower latency and stronger consistency. Synchronous cross-region replication is a good example: there may be no failure, but stronger coordination increases write latency.</p></div>"},{"title":"56. Practical production scenario","body":"<p>Suppose an e-commerce system has:</p>\n<div class=\"code-block\"><span class=\"code-label\">Deployment</span><pre>Primary region: Mumbai\nSecondary region: Singapore</pre></div>\n<p>Operations can be classified this way:</p>\n<div class=\"code-block\"><span class=\"code-label\">Operation classification</span><pre>Product catalog\n    Singapore can serve stale copy\n    AP-friendly\n\nRecommendation feed\n    local region continues\n    AP\n\nInventory reservation\n    authoritative owner/quorum only\n    CP\n\nPayment authorization\n    authoritative region only\n    CP\n\nOrder-history page\n    stale replica acceptable\n    eventual consistency\n\nAccount-disabled check\n    strong/current\n    CP-style\n\nAnalytics dashboard\n    AP/eventual</pre></div>\n<p>Now the architecture has explicit semantics rather than one vague system-wide label.</p>"},{"title":"57. Production incident: dual-region oversell","body":"<p>Suppose stock:</p>\n<div class=\"code-block\"><span class=\"code-label\">Inventory before partition</span><pre>1 item</pre></div>\n<p>Mumbai and Singapore cannot communicate.</p>\n<p>Both remain active.</p>\n<p>Mumbai user buys:</p>\n<div class=\"code-block\"><span class=\"code-label\">Both regions sell</span><pre>last item</pre></div>\n<p>Singapore user simultaneously buys:</p>\n<div class=\"code-block\"><span class=\"code-label\">Both regions sell</span><pre>last item</pre></div>\n<p>Each local database sees:</p>\n<div class=\"code-block\"><span class=\"code-label\">Stale local state</span><pre>inventory = 1</pre></div>\n<p>Both decrement to zero.</p>\n<p>After reconciliation:</p>\n<div class=\"code-block\"><span class=\"code-label\">Oversell result</span><pre>two orders\none item</pre></div>\n<p>This is the concrete cost of choosing availability for a globally constrained resource.</p>\n<p>Possible fixes:</p>\n<ul><li>single-region ownership</li><li>quorum/consensus</li><li>preallocated regional inventory</li><li>reservation tokens</li><li>escrow allocation</li></ul>\n<p>The business invariant determines the right technique.</p>","diagram":"flowchart TD\n  Stock[Global stock = 1] --> P[Partition]\n  P --> M[Mumbai sees 1 and sells it]\n  P --> S[Singapore sees 1 and sells it]\n  M --> Heal[Reconcile]\n  S --> Heal\n  Heal --> Incident[Two orders, one item]"},{"title":"58. Production incident: CP outage mistaken for failure","body":"<p>A three-node database cluster:</p>\n<div class=\"code-block\"><span class=\"code-label\">Three-node cluster</span><pre>A\nB\nC</pre></div>\n<p>Partition:</p>\n<div class=\"code-block\"><span class=\"code-label\">Partition topology</span><pre>A | B C</pre></div>\n<p>A was primary.</p>\n<p>A stops accepting writes because it lost quorum.</p>\n<p>Operators see:</p>\n<div class=\"code-block\"><span class=\"code-label\">Operator observation</span><pre>A is healthy\nCPU low\ndisk healthy\nbut writes rejected</pre></div>\n<p>They conclude:</p>\n<div class=\"code-block\"><span class=\"code-label\">Incorrect conclusion</span><pre>database is broken</pre></div>\n<p>In fact, the system is behaving correctly.</p>\n<p>It is sacrificing availability to prevent split brain.</p>\n<p>Understanding CAP helps distinguish:</p>\n<div class=\"code-block\"><span class=\"code-label\">Correct interpretation</span><pre>safety mechanism</pre></div>\n<p>from:</p>\n<div class=\"code-block\"><pre>software bug</pre></div>","diagram":"flowchart TD\n  Split[Cluster partition: A isolated from B and C] --> A[A is alive but lacks quorum]\n  Split --> BC[B and C have majority]\n  A --> Reject[A rejects writes]\n  BC --> Continue[Majority continues]\n  Reject --> Safety[This is a safety mechanism, not necessarily a software bug]"},{"title":"59. Production incident: AP conflict hidden by last-write-wins","body":"<p>Two regions update a customer's configuration object.</p>\n<p>Mumbai changes:</p>\n<div class=\"code-block\"><span class=\"code-label\">Mumbai update</span><pre>language = hi</pre></div>\n<p>Singapore changes:</p>\n<div class=\"code-block\"><span class=\"code-label\">Singapore update</span><pre>notificationEnabled = false</pre></div>\n<p>Whole-object LWW chooses Singapore's version.</p>\n<p>Result:</p>\n<div class=\"code-block\"><span class=\"code-label\">Lost update</span><pre>language change disappears</pre></div>\n<p>The system technically converged.</p>\n<p>But business data was lost.</p>\n<p>Better approaches:</p>\n<ul><li>field-level updates</li><li>version vectors</li><li>mergeable data structure</li><li>explicit conflict resolution</li></ul>\n<p>\"Eventually consistent\" is not enough; convergence semantics matter.</p>","diagram":"flowchart TD\n  M[Mumbai updates language = hi] --> LWW[Whole-object last-write-wins]\n  S[Singapore updates notifications = false] --> LWW\n  LWW --> Winner[One object wins]\n  Winner --> Lost[The other valid field update disappears]"},{"title":"60. A practical decision framework","body":"<p>For any distributed operation, reason through these questions:</p>\n<ol><li>What invariant could be violated if two partitions act independently?</li><li>Can that invariant be repaired after the fact?</li><li>How expensive is temporary unavailability?</li><li>Can authority be partitioned so each side owns different keys?</li><li>Can resources be preallocated to avoid coordination?</li><li>Does the user require read-your-writes or full linearizability?</li><li>How stale may reads be?</li><li>Can conflicts be merged deterministically?</li><li>What happens when the partition heals?</li><li>How will reconciliation be audited?</li><li>What is the latency cost of strong coordination?</li><li>Is consistency required globally or only per key/tenant?</li><li>Can cache/replica layers weaken the storage guarantee?</li><li>What is the system's actual behavior during loss of quorum?</li><li>Is the CAP choice documented per operation?</li></ol>"},{"title":"The deeper principle and what comes next","body":"<p>The deeper principle is:</p>\n<div class=\"callout\"><p><strong>CAP is not a menu where architects choose two letters. It is a failure-policy question: when parts of your distributed system cannot communicate, do you refuse work because you cannot guarantee a single truth, or continue work knowing that multiple truths may temporarily exist?</strong></p></div>\n<p>For money, uniqueness, permissions, and scarce inventory, refusing uncertain operations is often safer. For feeds, carts, analytics, counters, and other mergeable state, continuing and reconciling can be the better product decision.</p>\n<p>The next topic is <strong>Eventual Consistency</strong>: propagation delay, convergence, read-your-writes, monotonic reads, causal consistency, conflict resolution, reconciliation, anti-entropy, version vectors, out-of-order events, stale caches, idempotent consumers, and how to design user experiences around temporary inconsistency.</p>"}]);
window.FULL_LESSONS['day-34-cap-theorem'].keyTakeaways = ["CAP = distributed-system behavior during network partition.","Consistency = usually linearizability.","Availability = every request to a non-failed node can be serviced.","Partition tolerance = the system continues reasoning despite communication splits.","The real choice during a partition is consistency or availability.","CP = refuse unsafe operations.","AP = continue locally and reconcile later.","CAP consistency is not ACID consistency.","A partition is not only a broken network cable; slow or unreachable nodes can behave similarly.","Quorum = overlapping authorities can preserve stronger consistency.","AP does not mean random incorrectness.","CP does not mean permanently unavailable.","PACELC = during partition choose availability or consistency; otherwise choose latency or consistency.","The best choice is determined by the business invariant.","One application can contain both CP and AP operations."];
