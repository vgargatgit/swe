// Day 1 full lesson, part 1 of 4.
window.FULL_LESSONS=window.FULL_LESSONS||{};window.FULL_LESSONS['day-01-rate-limiting']={day:1,title:'Rate Limiting',subtitle:'Rate limiting is not just “block too many requests.” It controls who may consume which resources, at what cost, and how quickly.',tags:['Rate limiting','Token bucket','Sliding window','Redis','HTTP 429','Trusted proxies','Spring Boot'],core:'Who is making the request, what resource are they using, how costly is it, and how much should they be allowed to consume?',sections:[{title:'What rate limiting really solves',body:`<p>Rate limiting is not just “block too many requests.”</p><p>It protects your system from:</p><ul><li>accidental client bugs</li><li>brute force attacks</li><li>credential stuffing</li><li>scraping</li><li>abusive tenants</li><li>retry storms</li><li>expensive API misuse</li><li>sudden traffic spikes</li><li>noisy-neighbor problems in multi-tenant systems</li></ul><p>A good rate limiter answers:</p><div class='callout'><p>“Who is making the request, what resource are they using, how costly is it, and how much should they be allowed to consume?”</p></div><p>That “who” is extremely important.</p><p>It could be:</p><table><thead><tr><th>Client identity</th><th>Example</th></tr></thead><tbody><tr><td>IP address</td><td>unauthenticated APIs</td></tr><tr><td>user ID</td><td>logged-in user</td></tr><tr><td>API key</td><td>public developer APIs</td></tr><tr><td>tenant/org ID</td><td>SaaS systems</td></tr><tr><td>device ID</td><td>mobile apps</td></tr><tr><td>session ID</td><td>browser apps</td></tr><tr><td>endpoint</td><td><span class='inline-code'>/login</span>, <span class='inline-code'>/search</span>, <span class='inline-code'>/export</span></td></tr><tr><td>combination</td><td><span class='inline-code'>userId + endpoint</span>, <span class='inline-code'>tenantId + API</span></td></tr></tbody></table><p>In production, rate limiting is usually not one global rule. It is a set of layered rules.</p><p>Example:</p><div class='code-block'><span class='code-label'>text</span><pre>/login:&#10;  5 requests/minute per IP&#10;  10 requests/hour per username&#10;  50 requests/hour per device&#10;&#10;/search:&#10;  60 requests/minute per user&#10;  1000 requests/minute per tenant&#10;&#10;/export:&#10;  3 requests/hour per user&#10;  20 requests/day per tenant&#10;</pre></div>`},{title:'Where rate limiting can be applied',diagram:'flowchart LR; Client[Client] --> Edge[CDN / WAF]; Edge --> Gateway[Load balancer / reverse proxy / API gateway]; Gateway --> App[Application-aware limits]; App --> Resource[Database / job / expensive resource]',body:`<h3>Option 1: CDN / WAF layer</h3><p>Example: Cloudflare, AWS WAF, Akamai, Fastly.</p><p>Good for:</p><ul><li>DDoS protection</li><li>IP-based blocking</li><li>bot protection</li><li>geo/IP reputation</li><li>coarse limits before traffic reaches your infra</li></ul><p>Weak for:</p><ul><li>user-specific limits</li><li>tenant-specific limits</li><li>business-aware limits</li><li>request-cost-aware limits</li></ul><p>At this layer, the system may not know the authenticated user.</p><h3>Option 2: Load balancer / reverse proxy</h3><p>Example: NGINX, Envoy, HAProxy.</p><p>Good for:</p><ul><li>protecting backend services</li><li>cheap request throttling</li><li>IP or header-based limits</li><li>endpoint-level throttling</li></ul><p>Weak for:</p><ul><li>complex business rules</li><li>per-user limits after JWT/session decoding unless configured carefully</li></ul><p>Example NGINX style:</p><div class='code-block'><span class='code-label'>nginx</span><pre>limit_req_zone $binary_remote_addr zone=login_limit:10m rate=5r/m;&#10;&#10;server {&#10;    location /api/v1/login {&#10;        limit_req zone=login_limit burst=10 nodelay;&#10;        proxy_pass http://backend;&#10;    }&#10;}&#10;</pre></div><p>This says:</p><div class='code-block'><span class='code-label'>text</span><pre>Allow 5 requests per minute per client IP.&#10;Allow short burst up to 10.&#10;After that, reject or delay.&#10;</pre></div><h3>Option 3: API Gateway</h3><p>Example: Kong, Apigee, AWS API Gateway, Spring Cloud Gateway.</p><p>Good for:</p><ul><li>API key-based limits</li><li>per-customer limits</li><li>external developer APIs</li><li>central enforcement</li><li>quota management</li></ul><p>Weak for:</p><ul><li>very deep domain-specific rules</li><li>cases where backend alone knows the operation cost</li></ul><h3>Option 4: Application layer</h3><p>Example: Java/Spring Boot service using Redis.</p><p>Good for:</p><ul><li>per-user limits</li><li>tenant-aware limits</li><li>endpoint-specific limits</li><li>business-specific rules</li><li>dynamic limits based on plan/tier</li></ul><p>Weak for:</p><ul><li>traffic has already reached your service</li><li>adds app complexity</li><li>needs distributed consistency if service is horizontally scaled</li></ul><p>For most serious backend systems, you use multiple layers:</p><div class='code-block'><span class='code-label'>text</span><pre>WAF/CDN       -&gt; block obvious abuse&#10;NGINX/Gateway -&gt; coarse endpoint/IP limits&#10;Application  -&gt; user/tenant/business-aware limits&#10;Database/job -&gt; protect expensive operations&#10;</pre></div>`},{title:'Common rate limiting algorithms',body:`<h3>3.1 Fixed window counter</h3><p>Example:</p><div class='code-block'><span class='code-label'>text</span><pre>Allow 100 requests per minute.&#10;Counter key: user:123:2026-07-05T13:15&#10;</pre></div><p>If count &lt;= 100, allow. Else block.</p><p>Simple Redis implementation idea:</p><div class='code-block'><span class='code-label'>text</span><pre>INCR rate:user:123:minute:202607051315&#10;EXPIRE key 60 seconds&#10;</pre></div><h4>Problem</h4><p>Boundary burst.</p><p>A client can send:</p><div class='code-block'><span class='code-label'>text</span><pre>100 requests at 12:00:59&#10;100 requests at 12:01:00&#10;</pre></div><p>That becomes 200 requests in 1 second, even though the limit says 100/minute.</p><p>Good for:</p><ul><li>simple low-risk limits</li><li>daily quotas</li><li>rough protection</li></ul><p>Not ideal for:</p><ul><li>login</li><li>payment APIs</li><li>expensive APIs</li><li>strict fairness</li></ul><h3>3.2 Sliding window log</h3><p>Store every request timestamp.</p><p>Example:</p><div class='code-block'><span class='code-label'>text</span><pre>user:123 -&gt; [12:00:01, 12:00:05, 12:00:07, ...]&#10;</pre></div><p>On each request:</p><ol><li>Remove timestamps older than window.</li><li>Count remaining.</li><li>If count &lt; limit, allow.</li><li>Add current timestamp.</li></ol><p>Very accurate.</p><h4>Problem</h4><p>Memory expensive.</p><p>If many users send many requests, storing every timestamp becomes costly.</p><p>Good for:</p><ul><li>sensitive APIs</li><li>low-volume but high-risk operations</li><li>login attempts</li><li>OTP generation</li></ul><h3>3.3 Sliding window counter</h3><p>A compromise between fixed window and sliding log.</p><p>Instead of storing every timestamp, keep current window and previous window counts.</p><p>Example:</p><div class='code-block'><span class='code-label'>text</span><pre>Previous minute count = 80&#10;Current minute count = 30&#10;Current minute progress = 25%&#10;</pre></div><p>Weighted count:</p><div class='code-block'><span class='code-label'>text</span><pre>effective_count = current_count + previous_count * remaining_window_fraction&#10;</pre></div><p>If 25% of current minute has passed, 75% of previous window still matters:</p><div class='code-block'><span class='code-label'>text</span><pre>effective_count = 30 + 80 * 0.75&#10;                = 90&#10;</pre></div><p>Better than fixed window, cheaper than sliding log.</p><p>Good for:</p><ul><li>APIs requiring smoother limits</li><li>high-scale services</li></ul><h3>3.4 Token bucket</h3><p>Think of a bucket that fills with tokens.</p><p>Example:</p><div class='code-block'><span class='code-label'>text</span><pre>Bucket capacity = 100 tokens&#10;Refill rate = 10 tokens/second&#10;Each request costs 1 token&#10;</pre></div><p>If token exists, allow request and remove token.</p><p>If no token, reject or delay.</p><p>This allows bursts but controls average rate.</p><p>Example:</p><div class='code-block'><span class='code-label'>text</span><pre>A user was idle for 10 seconds.&#10;Bucket fills up.&#10;They can now burst briefly.&#10;But after burst, they are limited by refill rate.&#10;</pre></div><p>Good for:</p><ul><li>public APIs</li><li>user-facing APIs</li><li>traffic with natural bursts</li><li>APIs where short burst is acceptable</li></ul><p>This is probably the most commonly useful mental model.</p><h3>3.5 Leaky bucket</h3><p>Requests enter a queue/bucket and leak out at a fixed rate.</p><p>Example:</p><div class='code-block'><span class='code-label'>text</span><pre>Accept incoming requests.&#10;Process at 10 requests/second.&#10;If queue full, reject.&#10;</pre></div><p>This smooths traffic.</p><p>Good for:</p><ul><li>background processing</li><li>message ingestion</li><li>smoothing downstream pressure</li></ul><p>Less suitable when you need immediate rejection behavior.</p>`},{title:'Token bucket example',diagram:'flowchart LR; Idle[Idle time] --> Refill[Tokens refill up to capacity]; Refill --> Request{Request arrives}; Request -- token available --> Allow[Allow and consume token]; Request -- no token --> Block[Reject or delay]; Allow --> Refill',body:`<p>Suppose:</p><div class='code-block'><span class='code-label'>text</span><pre>capacity = 10&#10;refill rate = 1 token/second&#10;</pre></div><p>At time <span class='inline-code'>T</span>, user has 10 tokens.</p><p>They send 10 requests immediately:</p><div class='code-block'><span class='code-label'>text</span><pre>allowed = 10&#10;tokens left = 0&#10;</pre></div><p>Then they send another request immediately:</p><div class='code-block'><span class='code-label'>text</span><pre>blocked&#10;</pre></div><p>After 3 seconds:</p><div class='code-block'><span class='code-label'>text</span><pre>tokens restored = 3&#10;allowed requests = 3&#10;</pre></div><p>This gives a nice production behavior:</p><div class='code-block'><span class='code-label'>text</span><pre>Small burst? Fine.&#10;Sustained abuse? Blocked.&#10;</pre></div>`}],keyTakeaways:[]};