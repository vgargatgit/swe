// Day 1 full lesson, part 4 of 4.
(()=>{const lesson=window.FULL_LESSONS['day-01-rate-limiting'];lesson.sections.push({title:'Common mistakes',body:`<h3>Mistake 1: One global limit</h3><div class='code-block'><span class='code-label'>text</span><pre>100 requests/minute for everyone&#10;</pre></div><p>Too crude.</p><p>Different endpoints need different limits.</p><h3>Mistake 2: Only IP-based limit</h3><p>Works poorly for authenticated systems and SaaS.</p><p>Use user/tenant-aware limits.</p><h3>Mistake 3: Trusting <span class='inline-code'>X-Forwarded-For</span> blindly</h3><p>Attackers can spoof it unless sanitized by trusted infra.</p><h3>Mistake 4: No <span class='inline-code'>Retry-After</span></h3><p>Clients do not know when to retry.</p><h3>Mistake 5: Same limit for cheap and expensive APIs</h3><p><span class='inline-code'>GET /health</span> and <span class='inline-code'>POST /export</span> should not cost the same.</p><h3>Mistake 6: No observability</h3><p>A limiter without logs and metrics becomes mysterious in production.</p>`},{title:'Mental model to remember',diagram:'flowchart TD; Limit[Rate-limit decision] --> Who[Who? IP / user / tenant / API key]; Limit --> What[What? endpoint / operation / resource]; Limit --> Cost[How costly? read / query / write / export]; Limit --> Time[How long? second / minute / hour / day / month]; Limit --> Action[What response? allow / delay / reject / queue / degrade]',body:`<p>Think of rate limiting as a layered defense:</p><div class='code-block'><span class='code-label'>text</span><pre>Edge protects infrastructure.&#10;Gateway protects services.&#10;Application protects business rules.&#10;Database/job queues protect expensive resources.&#10;</pre></div><p>And think of limits in dimensions:</p><div class='code-block'><span class='code-label'>text</span><pre>Who?&#10;  IP, user, tenant, API key&#10;&#10;What?&#10;  endpoint, operation, resource&#10;&#10;How costly?&#10;  cheap read, expensive query, write, export&#10;&#10;How long?&#10;  per second, minute, hour, day, month&#10;&#10;What response?&#10;  allow, delay, reject, queue, degrade&#10;</pre></div>`},{title:'Interview-style answer',body:`<p>If asked, “Design rate limiting for an API,” a strong answer is:</p><div class='callout good'><p>I would first identify the caller dimension: IP for anonymous traffic, user/API key for authenticated traffic, and tenant for SaaS fairness. I would apply coarse limits at CDN/WAF or gateway level and business-aware limits inside the application. For algorithms, I would use token bucket for burst-friendly APIs, sliding window for sensitive endpoints like login, and quotas for daily/monthly usage. I would store counters in Redis using atomic Lua scripts to work across multiple app instances. I would return HTTP 429 with Retry-After, expose RateLimit headers where safe, and monitor blocked requests, Redis failures, and per-tenant usage. I would also handle proxy headers carefully and never blindly trust X-Forwarded-For.</p></div>`},{title:'Today’s practical exercise',body:`<p>Design limits for this API set:</p><div class='code-block'><span class='code-label'>text</span><pre>POST /login&#10;POST /otp/send&#10;GET  /api/v1/user/current&#10;GET  /api/v1/search&#10;POST /api/v1/payment&#10;POST /api/v1/export&#10;</pre></div><p>For each one, decide:</p><div class='code-block'><span class='code-label'>text</span><pre>identity key&#10;algorithm&#10;limit&#10;window&#10;response behavior&#10;whether to fail open or fail closed&#10;</pre></div><p>A reasonable starting answer:</p><table><thead><tr><th>API</th><th>Key</th><th>Algorithm</th><th>Limit</th></tr></thead><tbody><tr><td><span class='inline-code'>/login</span></td><td>IP + username</td><td>sliding window</td><td>5/min/IP, 10/hour/username</td></tr><tr><td><span class='inline-code'>/otp/send</span></td><td>phone/email + IP</td><td>sliding window</td><td>3/10 min</td></tr><tr><td><span class='inline-code'>/user/current</span></td><td>user ID</td><td>token bucket</td><td>120/min</td></tr><tr><td><span class='inline-code'>/search</span></td><td>user + tenant</td><td>token bucket</td><td>60/min/user, 2000/min/tenant</td></tr><tr><td><span class='inline-code'>/payment</span></td><td>user + idempotency key</td><td>token bucket + idempotency</td><td>10/min</td></tr><tr><td><span class='inline-code'>/export</span></td><td>user + tenant</td><td>quota</td><td>5/hour/user, 50/day/tenant</td></tr></tbody></table><p class='callout good'>Tomorrow’s topic: <strong>Caching</strong>.</p>`});lesson.keyTakeaways=['Use layered limits: edge controls infrastructure abuse, gateways protect services, and application rules protect users, tenants, and expensive operations.','Choose the identity key deliberately: IP for anonymous traffic, user or API key for authenticated traffic, and tenant for SaaS fairness.','Use token bucket for burst-friendly APIs, sliding windows for sensitive operations, and quotas for daily or monthly usage.','Use shared Redis state and atomic Lua scripts when multiple application instances enforce one distributed limit.','Return HTTP 429 with Retry-After, design client retries with exponential backoff and jitter, and monitor limiter behavior.','Trust forwarded client-IP headers only when trusted infrastructure has sanitized and supplied them.'];})();