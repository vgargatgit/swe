window.FULL_LESSONS = window.FULL_LESSONS || {};
window.FULL_LESSONS["day-16-pubsub"] = {
  "day": 16,
  "title": "Pub/Sub",
  "subtitle": "Publish one event to many independent subscribers with their own delivery state.",
  "tags": [
    "Pub/Sub",
    "Topics",
    "Subscriptions",
    "Kafka",
    "Consumer groups",
    "Replay"
  ],
  "core": "Pub/Sub announces a fact once and gives independent subscriber groups their own delivery and recovery state.",
  "sections": [
    {
      "title": "Overview",
      "diagram": "flowchart LR\n  Producer --> Topic[(Topic)]\n  Topic --> Email[Email subscription]\n  Topic --> Analytics[Analytics subscription]\n  Topic --> Audit[Audit subscription]\n  Email --> EW[Competing email workers]\n  Analytics --> AW[Competing analytics workers]",
      "body": "<p>Yesterday we covered <strong>Message Queues</strong>, where the dominant mental model was:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Producer\n   │\n   ▼\n Queue\n   │\n   ├── Consumer 1\n   ├── Consumer 2\n   └── Consumer 3\n\nOne message → one logical consumer\n</code></pre></div>\n<p>Today we change one fundamental assumption.</p>\n<p>With <strong>Publish/Subscribe</strong>:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>                  ┌──► Email Service\n                  │\nOrder Service ──► Topic ──► Analytics Service\n                  │\n                  ├──► Loyalty Service\n                  │\n                  └──► Audit Service\n\nOne event → many independent subscribers\n</code></pre></div>\n<p>The publisher announces:</p>\n<div class=\"callout\">\n<p>\"Something happened.\"</p>\n</div>\n<p>It does not need to know who is interested.</p>\n<p>This seemingly small difference is the foundation of much of modern event-driven architecture.</p>"
    },
    {
      "title": "1. The core idea",
      "diagram": null,
      "body": "<p>Suppose the Order Service completes an order.</p>\n<p>Without Pub/Sub:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Order Service\n   │\n   ├── POST Email Service\n   ├── POST Analytics Service\n   ├── POST Loyalty Service\n   └── POST Audit Service\n</code></pre></div>\n<p>The Order Service now knows about four downstream systems.</p>\n<p>Add Fraud Analytics:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Order Service\n   │\n   └── POST Fraud Analytics\n</code></pre></div>\n<p>The producer must change.</p>\n<p>With Pub/Sub:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Order Service\n       │\n       │ publish\n       ▼\n   OrderCreated\n       │\n       ├──► Email subscription\n       │\n       ├──► Analytics subscription\n       │\n       ├──► Loyalty subscription\n       │\n       └──► Audit subscription\n</code></pre></div>\n<p>Later, Fraud Analytics subscribes:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>       OrderCreated\n           │\n           └──► Fraud Analytics\n</code></pre></div>\n<p>The Order Service doesn't change.</p>\n<p>This is <strong>consumer decoupling</strong>.</p>"
    },
    {
      "title": "2. Queue vs Pub/Sub",
      "diagram": null,
      "body": "<p>The most important distinction:</p>\n<h5>Work queue</h5>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>       Job\n        │\n        ▼\n      Queue\n     /  |  \\\n   C1   C2   C3\n\nExactly one consumer instance\nshould perform the logical work.\n</code></pre></div>\n<p>Example:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>GenerateThumbnail(videoId=123)\n</code></pre></div>\n<p>If you have ten workers, you don't want ten thumbnails generated.</p>\n<p>The workers are <strong>competing consumers</strong>.</p>\n\n<h5>Pub/Sub</h5>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>             Event\n               │\n               ▼\n              Topic\n          /      |      \\\n         /       |       \\\n        ▼        ▼        ▼\n     Email    Analytics   Audit\n</code></pre></div>\n<p>Each logical subscriber gets the event.</p>\n<p>Example:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>OrderCreated(orderId=123)\n</code></pre></div>\n<p>Each subscriber has independent work to do.</p>\n\n<p>The distinction is:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Queue:\n\"Someone please do this.\"\n\nPub/Sub:\n\"This happened; anyone interested may react.\"\n</code></pre></div>\n<p>This closely maps to:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Command → Queue\n\nEvent → Pub/Sub\n</code></pre></div>\n<p>Not universally, but it is an excellent design heuristic.</p>"
    },
    {
      "title": "3. Topic and subscription",
      "diagram": null,
      "body": "<p>Most Pub/Sub systems introduce two abstractions:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Topic\nSubscription\n</code></pre></div>\n<p>Producer publishes to:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>orders\n</code></pre></div>\n<p>Subscribers create subscriptions:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>orders-email\norders-analytics\norders-audit\n</code></pre></div>\n<p>Architecture:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>                    Topic: orders\n                         │\n           ┌─────────────┼─────────────┐\n           │             │             │\n           ▼             ▼             ▼\n     Subscription   Subscription   Subscription\n        email         analytics       audit\n           │             │             │\n           ▼             ▼             ▼\n       EmailSvc      AnalyticsSvc    AuditSvc\n</code></pre></div>\n<p>The crucial point is that each subscription has its <strong>own delivery state</strong>.</p>\n<p>Suppose Analytics is down.</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Email       ✓ processed\nAudit       ✓ processed\nAnalytics   ✗ unavailable\n</code></pre></div>\n<p>The Analytics subscription retains its backlog.</p>\n<p>Email and Audit continue independently.</p>\n<p>This is a major advantage over synchronous fan-out.</p>"
    },
    {
      "title": "4. Pub/Sub does not necessarily mean broadcast to every process",
      "diagram": null,
      "body": "<p>Suppose Analytics Service has five instances:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Analytics Subscription\n        │\n        ├── Analytics-1\n        ├── Analytics-2\n        ├── Analytics-3\n        ├── Analytics-4\n        └── Analytics-5\n</code></pre></div>\n<p>You generally don't want every instance to process every event.</p>\n<p>Instead:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>One event\n   ↓\nAnalytics subscription\n   ↓\nONE Analytics instance\n</code></pre></div>\n<p>At the same time:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>One event\n   │\n   ├── Email subscription     → one Email instance\n   │\n   ├── Analytics subscription → one Analytics instance\n   │\n   └── Audit subscription     → one Audit instance\n</code></pre></div>\n<p>So there are <strong>two levels</strong>:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Between subscriber groups:\n    fan-out\n\nWithin a subscriber group:\n    competing consumers\n</code></pre></div>\n<p>This is an extremely important mental model.</p>"
    },
    {
      "title": "5. Concrete example",
      "diagram": null,
      "body": "<p>Imagine:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>PaymentAuthorized\n</code></pre></div>\n<p>Subscribers:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>PaymentAuthorized\n       │\n       ├──► Order Service\n       │       mark order PAID\n       │\n       ├──► Notification Service\n       │       send receipt\n       │\n       ├──► Analytics Service\n       │       record conversion\n       │\n       └──► Fraud Service\n               update fraud model\n</code></pre></div>\n<p>Suppose Notification Service is down for two hours.</p>\n<p>Ideally:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Order Service       continues\nAnalytics           continues\nFraud               continues\n\nNotification backlog accumulates\n</code></pre></div>\n<p>When Notification recovers:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Notification\n     ↓\nprocess backlog\n</code></pre></div>\n<p>The failure is isolated to one subscriber.</p>"
    },
    {
      "title": "6. A subtle architectural benefit",
      "diagram": null,
      "body": "<p>With synchronous communication:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Order Service → Email Service\n</code></pre></div>\n<p>Order Service knows:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Email Service exists.\n</code></pre></div>\n<p>With Pub/Sub:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Order Service → OrderCreated\n</code></pre></div>\n<p>Order Service only knows:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>An order was created.\n</code></pre></div>\n<p>This is a different kind of coupling.</p>\n<p>You have moved from:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>service coupling\n</code></pre></div>\n<p>to:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>event/schema coupling\n</code></pre></div>\n<p>You have not eliminated coupling.</p>\n<p>That is an important system-design insight.</p>\n<p>The producer and consumer are now coupled through:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>event name\nevent meaning\nevent schema\ndelivery semantics\n</code></pre></div>\n<p>Poorly designed events can create just as much architectural pain as poorly designed APIs.</p>"
    },
    {
      "title": "7. Events should describe facts",
      "diagram": null,
      "body": "<p>Good:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>OrderCreated\nPaymentAuthorized\nWalletCredited\nStudentJoinedClass\nMessageSent\n</code></pre></div>\n<p>These describe something that happened.</p>\n<p>Less ideal:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>SendOrderEmail\nUpdateAnalytics\nNotifyLoyaltySystem\n</code></pre></div>\n<p>Those are commands disguised as events.</p>\n<p>Why does this matter?</p>\n<p>Suppose Order Service publishes:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>SendOrderEmail\n</code></pre></div>\n<p>Now the producer knows what a consumer should do.</p>\n<p>That creates coupling.</p>\n<p>Better:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>OrderCreated\n</code></pre></div>\n<p>Email Service decides:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>When OrderCreated occurs,\nI send an email.\n</code></pre></div>\n<p>Analytics independently decides:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>When OrderCreated occurs,\nI update analytics.\n</code></pre></div>"
    },
    {
      "title": "8. Event ownership",
      "diagram": null,
      "body": "<p>Who should publish:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>PaymentAuthorized\n</code></pre></div>\n<p>Usually the service that owns the payment state.</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Payment Service\n</code></pre></div>\n<p>Not:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Order Service\n</code></pre></div>\n<p>The general principle:</p>\n<div class=\"callout\">\n<p>The service that owns the state transition should publish the event representing that transition.</p>\n</div>\n<p>Otherwise you risk events that claim something happened when the authoritative service disagrees.</p>"
    },
    {
      "title": "9. Events should usually contain IDs, but there is a trade-off",
      "diagram": null,
      "body": "<p>Consider:</p>\n<div class=\"code-block\"><span class=\"code-label\">json</span><pre><code>{\n  \"eventType\": \"OrderCreated\",\n  \"orderId\": \"O123\"\n}\n</code></pre></div>\n<p>Consumer needs details.</p>\n<p>It calls:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Order Service\n</code></pre></div>\n<p>Now:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Event\n  ↓\nConsumer\n  ↓\nHTTP GET /orders/O123\n</code></pre></div>\n<p>This reintroduces runtime coupling.</p>\n<p>If Order Service is unavailable, consumer cannot process.</p>\n<p>Alternative:</p>\n<div class=\"code-block\"><span class=\"code-label\">json</span><pre><code>{\n  \"eventType\": \"OrderCreated\",\n  \"orderId\": \"O123\",\n  \"customerId\": \"C456\",\n  \"amount\": 5000,\n  \"currency\": \"INR\"\n}\n</code></pre></div>\n<p>Consumer can work independently.</p>\n<p>But now:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>larger events\nschema evolution\nduplicated data\npotential PII exposure\n</code></pre></div>\n<p>This is the classic:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>event notification\nvs\nevent-carried state transfer\n</code></pre></div>\n<p>trade-off.</p>"
    },
    {
      "title": "10. Event Notification",
      "diagram": null,
      "body": "<p>Small event:</p>\n<div class=\"code-block\"><span class=\"code-label\">json</span><pre><code>{\n  \"eventType\": \"OrderCreated\",\n  \"orderId\": \"O123\"\n}\n</code></pre></div>\n<p>Meaning:</p>\n<div class=\"callout\">\n<p>Something changed. Ask the owner if you need details.</p>\n</div>\n<p>Advantages:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>small messages\nless duplicated data\nsimpler schemas\n</code></pre></div>\n<p>Disadvantages:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>consumer must call producer\nruntime coupling returns\nproducer load increases\n</code></pre></div>"
    },
    {
      "title": "11. Event-Carried State Transfer",
      "diagram": null,
      "body": "<p>Event contains enough data:</p>\n<div class=\"code-block\"><span class=\"code-label\">json</span><pre><code>{\n  \"eventType\": \"OrderCreated\",\n  \"orderId\": \"O123\",\n  \"customerId\": \"C456\",\n  \"amount\": 5000,\n  \"currency\": \"INR\"\n}\n</code></pre></div>\n<p>Consumer may maintain its own local representation.</p>\n<p>Advantages:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>consumer independence\nfewer synchronous calls\nbetter resilience\n</code></pre></div>\n<p>Disadvantages:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>data duplication\nevent schema complexity\neventual consistency\nprivacy/security concerns\n</code></pre></div>\n<p>Neither is universally correct.</p>"
    },
    {
      "title": "12. Delivery semantics still matter",
      "diagram": null,
      "body": "<p>Pub/Sub doesn't eliminate yesterday's problems.</p>\n<p>You still need to ask:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Can events be duplicated?\nCan events arrive out of order?\nCan events be lost?\nCan consumers replay events?\nHow long are events retained?\n</code></pre></div>\n<p>Most production architectures should assume:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>at-least-once delivery\n</code></pre></div>\n<p>Therefore:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Event E123\n   ↓\nConsumer processes\n   ↓\nACK lost\n   ↓\nEvent E123 delivered again\n</code></pre></div>\n<p>Consumer must be idempotent.</p>\n<p>Exactly the same lesson as queues.</p>"
    },
    {
      "title": "13. Subscriber idempotency is independent",
      "diagram": null,
      "body": "<p>Suppose:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>eventId = E123\n</code></pre></div>\n<p>Email Service processes it.</p>\n<p>Analytics Service processes it.</p>\n<p>Their deduplication state must be independent.</p>\n<p>Conceptually:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Email:\nprocessed:E123\n\nAnalytics:\nprocessed:E123\n\nAudit:\nprocessed:E123\n</code></pre></div>\n<p>You don't want a global:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>processed:E123\n</code></pre></div>\n<p>because Email processing the event should not prevent Analytics from processing it.</p>\n<p>Deduplication belongs to:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>subscriber + event\n</code></pre></div>\n<p>Conceptually:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>(email-service, E123)\n(analytics-service, E123)\n(audit-service, E123)\n</code></pre></div>"
    },
    {
      "title": "14. Slow subscriber problem",
      "diagram": null,
      "body": "<p>Suppose:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Publisher: 10,000 events/sec\n\nEmail subscriber:     10,000/sec\nAnalytics subscriber: 15,000/sec\nAudit subscriber:      1,000/sec\n</code></pre></div>\n<p>Audit cannot keep up.</p>\n<p>Its backlog grows:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>9,000 events/sec\n</code></pre></div>\n<p>Other subscribers should remain unaffected.</p>\n<p>But eventually Audit faces:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>storage limits\nretention expiration\nhours of lag\nexpensive backlog recovery\n</code></pre></div>\n<p>Therefore monitor <strong>subscriber lag independently</strong>.</p>\n<p>For each subscription:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>backlog size\noldest event age\nprocessing rate\nfailure rate\n</code></pre></div>"
    },
    {
      "title": "15. Subscriber outage",
      "diagram": null,
      "body": "<p>Suppose:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Audit Service down for 3 days\n</code></pre></div>\n<p>What happens?</p>\n<p>Depends on the broker.</p>\n<p>If retention is:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>1 day\n</code></pre></div>\n<p>some events may disappear before Audit returns.</p>\n<p>If audit events are legally important, this is unacceptable.</p>\n<p>So retention must be chosen based on:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>maximum tolerated subscriber outage\n</code></pre></div>\n<p>and business requirements.</p>\n<p>For critical audit data, the event broker may not be your permanent system of record.</p>"
    },
    {
      "title": "16. Push vs pull subscriptions",
      "diagram": null,
      "body": "<p>There are two broad delivery models.</p>\n<h5>Push</h5>\n<p>Broker calls consumer:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Broker\n  │\n  │ HTTP POST\n  ▼\nConsumer\n</code></pre></div>\n<p>Advantages:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>simple consumer\nlow latency\n</code></pre></div>\n<p>Problems:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>consumer must expose endpoint\nbroker may overwhelm consumer\nretry behavior matters\n</code></pre></div>\n\n<h5>Pull</h5>\n<p>Consumer asks broker:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Consumer\n   │\n   │ Give me messages\n   ▼\nBroker\n</code></pre></div>\n<p>Advantages:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>consumer controls consumption rate\nnatural backpressure\neasier worker scaling\n</code></pre></div>\n<p>Problems:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>consumer manages polling\nbatch sizing\nacknowledgments\n</code></pre></div>\n<p>Neither is inherently superior.</p>"
    },
    {
      "title": "17. Filtering",
      "diagram": null,
      "body": "<p>Suppose topic:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>OrderEvents\n</code></pre></div>\n<p>contains:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>OrderCreated\nOrderPaid\nOrderShipped\nOrderCancelled\n</code></pre></div>\n<p>Notification Service only wants:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>OrderShipped\n</code></pre></div>\n<p>You can:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>subscribe to everything\nand filter in consumer\n</code></pre></div>\n<p>or use broker-level filtering.</p>\n<p>Broker filtering reduces:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>network traffic\nconsumer CPU\nunnecessary deliveries\n</code></pre></div>\n<p>But excessive filtering rules can make messaging topology difficult to understand.</p>"
    },
    {
      "title": "18. Topic design",
      "diagram": null,
      "body": "<p>A common question:</p>\n<p>Should we create:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>order-created\norder-paid\norder-shipped\n</code></pre></div>\n<p>or:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>order-events\n</code></pre></div>\n<p>There is no universal answer.</p>\n<p>Fine-grained topics:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>clear semantics\neasy permissions\neasy subscription\n</code></pre></div>\n<p>but:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>many topics\noperational complexity\n</code></pre></div>\n<p>Broad domain topics:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>fewer resources\nsimpler topology\n</code></pre></div>\n<p>but require:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>filtering\nschema discrimination\n</code></pre></div>\n<p>A useful compromise is domain-oriented topics:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>orders\npayments\nwallet\nnotifications\n</code></pre></div>\n<p>with:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>eventType\n</code></pre></div>\n<p>inside the envelope.</p>"
    },
    {
      "title": "19. Ordering in Pub/Sub",
      "diagram": null,
      "body": "<p>Suppose:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>E1: WalletCreated\nE2: WalletCredited ₹100\nE3: WalletDebited ₹50\n</code></pre></div>\n<p>If consumer receives:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>E3\nE1\nE2\n</code></pre></div>\n<p>things break.</p>\n<p>As with queues, avoid demanding global ordering.</p>\n<p>Instead use:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>partition key = walletId\n</code></pre></div>\n<p>Then:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>wallet-123:\nE1 → E2 → E3\n</code></pre></div>\n<p>while:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>wallet-456\n</code></pre></div>\n<p>can process independently.</p>\n<p>Ordering is generally most useful:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>per aggregate\nper account\nper order\nper wallet\nper user\n</code></pre></div>"
    },
    {
      "title": "20. The hot partition problem",
      "diagram": null,
      "body": "<p>Suppose partition key:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>tenantId\n</code></pre></div>\n<p>One giant tenant generates:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>80% of all traffic\n</code></pre></div>\n<p>Then:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>partition A → 80%\npartition B → 5%\npartition C → 5%\n...\n</code></pre></div>\n<p>You have a hot partition.</p>\n<p>Ordering requirements and scalability often conflict.</p>\n<p>The broader the ordering scope:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>more ordering\nless parallelism\n</code></pre></div>\n<p>The narrower:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>more parallelism\nless ordering\n</code></pre></div>\n<p>Choose the smallest ordering scope required by business correctness.</p>"
    },
    {
      "title": "21. Kafka changes the mental model",
      "diagram": null,
      "body": "<p>With traditional brokers, we often think:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Message delivered\n    ↓\nACK\n    ↓\nMessage removed\n</code></pre></div>\n<p>With Apache Kafka, the dominant abstraction is different.</p>\n<p>Messages live in an append-only partitioned log:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Partition 0\n\noffset 0\noffset 1\noffset 2\noffset 3\noffset 4\n</code></pre></div>\n<p>Consumers track:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>current offset\n</code></pre></div>\n<p>Example:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Consumer Group A → offset 4\nConsumer Group B → offset 2\n</code></pre></div>\n<p>The message isn't deleted merely because one consumer processed it.</p>\n<p>Retention is independent.</p>\n<p>This enables:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>replay\nmultiple consumer groups\nhistorical processing\n</code></pre></div>"
    },
    {
      "title": "22. Consumer groups",
      "diagram": null,
      "body": "<p>Kafka combines queue and Pub/Sub semantics elegantly.</p>\n<p>Suppose topic:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>OrderEvents\n</code></pre></div>\n<p>Consumer groups:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Email Group\nAnalytics Group\nAudit Group\n</code></pre></div>\n<p>Each group receives all events.</p>\n<p>Within Email Group:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Email Worker 1\nEmail Worker 2\nEmail Worker 3\n</code></pre></div>\n<p>partition processing is distributed among workers.</p>\n<p>So:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Across groups:\nPub/Sub\n\nWithin group:\nCompeting consumers\n</code></pre></div>\n<p>This is exactly the two-level model we discussed earlier.</p>"
    },
    {
      "title": "23. Partition count limits consumer parallelism",
      "diagram": null,
      "body": "<p>Suppose:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Topic partitions = 3\n</code></pre></div>\n<p>Consumer group:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Consumer 1\nConsumer 2\nConsumer 3\nConsumer 4\nConsumer 5\n</code></pre></div>\n<p>Only three consumers can actively consume partitions.</p>\n<p>Conceptually:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Partition 1 → Consumer 1\nPartition 2 → Consumer 2\nPartition 3 → Consumer 3\n\nConsumer 4 → idle\nConsumer 5 → idle\n</code></pre></div>\n<p>Therefore:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>maximum useful consumer parallelism\n≈ number of partitions\n</code></pre></div>\n<p>This is a critical Kafka capacity-planning consideration.</p>"
    },
    {
      "title": "24. Rebalancing",
      "diagram": null,
      "body": "<p>Suppose:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Consumer 1 → Partition A\nConsumer 2 → Partition B\n</code></pre></div>\n<p>Consumer 2 crashes.</p>\n<p>The group rebalances:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Consumer 1 → A + B\n</code></pre></div>\n<p>Add Consumer 3:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Consumer 1 → A\nConsumer 3 → B\n</code></pre></div>\n<p>Rebalancing can temporarily pause or disrupt processing.</p>\n<p>Frequent consumer churn can cause:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>rebalance storms\nlatency spikes\nduplicate processing\n</code></pre></div>\n<p>Stable consumer deployments matter.</p>"
    },
    {
      "title": "25. Replay",
      "diagram": null,
      "body": "<p>One major advantage of log-based systems:</p>\n<p>Suppose you create a new recommendation engine.</p>\n<p>Historical events exist:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>2025\n2026\n</code></pre></div>\n<p>New consumer:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Recommendation Engine\n</code></pre></div>\n<p>can start from an old offset:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>replay historical OrderEvents\n</code></pre></div>\n<p>and build its state.</p>\n<p>Traditional ephemeral queues usually don't provide this model as naturally.</p>\n<p>This is one reason Kafka is often called an <strong>event streaming platform</strong>, not merely a message queue.</p>"
    },
    {
      "title": "26. Replay creates a new problem",
      "diagram": null,
      "body": "<p>Imagine replaying:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>OrderCreated\n</code></pre></div>\n<p>to Email Service.</p>\n<p>You probably don't want:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>100 million historical emails\n</code></pre></div>\n<p>So consumers must understand whether they are:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>processing live events\n</code></pre></div>\n<p>or:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>rebuilding derived state\n</code></pre></div>\n<p>Replay-friendly consumers generally avoid irreversible external side effects unless explicitly intended.</p>\n<p>Analytics:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>good replay candidate\n</code></pre></div>\n<p>Sending SMS:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>dangerous replay candidate\n</code></pre></div>"
    },
    {
      "title": "27. Dual-write problem still exists",
      "diagram": null,
      "body": "<p>Producer:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>BEGIN\n\nINSERT order\n\nCOMMIT\n\npublish OrderCreated\n</code></pre></div>\n<p>Crash between commit and publish:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Order exists\nEvent missing\n</code></pre></div>\n<p>Same problem as yesterday.</p>\n<p>Use:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Transactional Outbox\n</code></pre></div>\n<p>Conceptually:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>BEGIN\n\nINSERT INTO orders\n\nINSERT INTO outbox_events\n\nCOMMIT\n</code></pre></div>\n<p>Then publish asynchronously.</p>\n<p>Pub/Sub does not solve producer consistency automatically.</p>"
    },
    {
      "title": "28. Outbox + Pub/Sub",
      "diagram": null,
      "body": "<p>A robust architecture:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>             Order Service\n                  │\n                  ▼\n        ┌───────────────────┐\n        │ Database          │\n        │                   │\n        │ orders            │\n        │ outbox_events     │\n        └─────────┬─────────┘\n                  │\n                  ▼\n          Outbox Publisher\n                  │\n                  ▼\n            Order Topic\n          /       |       \\\n         ▼        ▼        ▼\n      Email   Analytics   Audit\n</code></pre></div>\n<p>Now:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>DB transaction\n</code></pre></div>\n<p>guarantees:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>order + intent-to-publish-event\n</code></pre></div>\n<p>are atomic.</p>\n<p>Consumers remain idempotent because the outbox publisher itself can publish duplicates.</p>"
    },
    {
      "title": "29. The event explosion problem",
      "diagram": null,
      "body": "<p>Pub/Sub makes adding consumers easy.</p>\n<p>That is both a strength and a danger.</p>\n<p>Initially:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>OrderCreated\n  ├── Email\n  └── Analytics\n</code></pre></div>\n<p>Years later:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>OrderCreated\n  ├── Email\n  ├── Analytics\n  ├── Loyalty\n  ├── Fraud\n  ├── CRM\n  ├── Data Lake\n  ├── Audit\n  ├── Recommendation\n  └── Marketing\n</code></pre></div>\n<p>Then someone changes:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>OrderCreated schema\n</code></pre></div>\n<p>and breaks five consumers nobody remembered existed.</p>\n<p>This is <strong>event contract governance</strong>.</p>\n<p>At scale you need:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>schema registry\ncompatibility rules\nevent ownership\nconsumer inventory\nversioning policy\ndeprecation process\n</code></pre></div>\n<p>Loose runtime coupling does not mean loose contract discipline.</p>"
    },
    {
      "title": "30. Don't expose internal database models as events",
      "diagram": null,
      "body": "<p>Bad:</p>\n<div class=\"code-block\"><span class=\"code-label\">json</span><pre><code>{\n  \"table\": \"orders\",\n  \"operation\": \"UPDATE\",\n  \"row\": {\n    \"...\": \"...\"\n  }\n}\n</code></pre></div>\n<p>Now consumers depend on your database schema.</p>\n<p>Refactor the database:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>consumer breaks\n</code></pre></div>\n<p>Better:</p>\n<div class=\"code-block\"><span class=\"code-label\">json</span><pre><code>{\n  \"eventType\": \"OrderShipped\",\n  \"orderId\": \"O123\",\n  \"shippedAt\": \"...\"\n}\n</code></pre></div>\n<p>Events should represent domain semantics, not accidental persistence structure.</p>\n<p>CDC—Change Data Capture—is useful, but raw database changes and domain events solve different problems.</p>"
    },
    {
      "title": "31. Pub/Sub and failures",
      "diagram": null,
      "body": "<p>Consider:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>OrderCreated\n  │\n  ├── Email       ✓\n  ├── Analytics   ✓\n  ├── Loyalty     ✗\n  └── Audit       ✓\n</code></pre></div>\n<p>Has <code class=\"inline-code\">OrderCreated</code> succeeded?</p>\n<p>That question is usually wrong.</p>\n<p>The event already happened.</p>\n<p>Each subscriber has its own processing state:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Email       succeeded\nAnalytics   succeeded\nLoyalty     retrying\nAudit       succeeded\n</code></pre></div>\n<p>There is no single distributed \"event transaction.\"</p>\n<p>If business correctness requires:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Order cannot complete unless Loyalty succeeds\n</code></pre></div>\n<p>then Loyalty probably should not merely be an optional asynchronous subscriber.</p>\n<p>This leads us toward upcoming topics:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Distributed Transactions\nSaga Pattern\n</code></pre></div>"
    },
    {
      "title": "32. Don't use Pub/Sub to hide synchronous dependencies",
      "diagram": null,
      "body": "<p>Suppose:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>CreateOrder\n</code></pre></div>\n<p>requires inventory reservation before confirming success.</p>\n<p>You could publish:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>ReserveInventory\n</code></pre></div>\n<p>and immediately return:</p>\n<div class=\"code-block\"><span class=\"code-label\">text</span><pre><code>Order confirmed\n</code></pre></div>\n<p>But inventory may late</p>\n<div class=\"callout warn\">\n<p>Extraction note: this chapter came from a ChatGPT reader page capped at 20,000 characters. It may need a later full-export verification pass.</p>\n</div>"
    }
  ],
  "keyTakeaways": [
    "Fan out across subscriber groups and compete within each group.",
    "Events describe facts owned by the service that owns the state transition.",
    "Choose notification versus event-carried state intentionally.",
    "Ordering, retention, replay, partitioning, and schema governance are subscriber contracts.",
    "Keep each subscriber independently idempotent and observable."
  ]
};
